'use client';

import Link from 'next/link';

export default function Subnav() {
  return (
    <nav aria-label="Sub navigation" className="hidden md:block">
      {/* placeholder; keep empty to avoid layout shifts */}
    </nav>
  );
}
